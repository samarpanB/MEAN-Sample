define(['angular'], function () {
  'use strict';

    return ['$scope', '$element', function (scope) {
        debugger;
        // Garbage collect 
        scope.$on('$destroy', function() {
            
        });
    }];
});